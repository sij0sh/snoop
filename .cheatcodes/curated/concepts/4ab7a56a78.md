---
cheatcodes_id: 4ab7a56a78
type: Runbook
title: Verify refactoring cuts with targeted sweeps and recompilation
description: Use targeted symbol and content sweeps, database-artifact checks, forced recompilation, and the full test suite to verify that migration and schema cleanup is complete without repository drift or loss of required functionality.
tags:
  - refactoring
  - migrations
  - schema
  - verification
  - testing
status: draft
generated:
  by: cheatcodes/0.2.0
  at: 2026-08-30T13:17:09.342Z
sources:
  - id: session-df69333d19c3dca1
    resource: session:d92261cc-3aa4-4195-b930-4102868d09da#entries=cd0cce5c,6c0d38c7
    title: Session evidence
---

# Purpose

Confirm that a migration/schema refactor leaves only the intended schema path, produces no unexpected files, and preserves migration behavior and test coverage.

# Steps

1. Check for files modified outside the intended target area and verify the tracked source file has not changed unexpectedly.
2. Check for unexpected database files such as the main database, shared-memory file, or write-ahead log.
3. Search the implementation for obsolete migration constants and dropped-table references; confirm only the intended squash comment remains.
4. Verify that INIT_SCHEMA is the sole schema definition and that migration_after maps the fresh-database path to it.
5. Inspect tests for the expected PRAGMA user_version value and run the full test suite.
6. Confirm compilation and tests succeed, including idempotent migration, vector loading, and concurrent migration of a shared fresh database.

# Evidence

- [evidence-c1d3960274ff33d01e2da3b2] --- drift: files modified after 07:49 (expect none outside target/) ---
--- store.rs mtime (expect 07:28:10 unchanged) ---
src/store.rs 57861 bytes mtime 2026-08-29 07:28:10.708215663 -0700
--- b.db* (expect absent) ---
ls: cannot access 'b.db': No such file or directory
ls: cannot access 'b.db-shm': No such file or directory
ls: cannot access 'b.db-wal': No such file or directory
--- migration constants / dropped tables (expect only squash comment) ---
src/store.rs:50:// Single init schema. Squashes the former MIGRATION_V1..V8 ladder; databases
migr_exit=0
--- INIT_SCHEMA as sole schema + migration_after mapping ---
52:const INIT_SCHEMA: &str = r#"
195:fn migration_after(version: i64) -> Option<(i64, &'static str)> {
197:        0 => Some((1, INIT_SCHEMA)),
--- user_version asserts in tests (expect == 1) ---
204:        let version: i64 = conn.query_row("PRAGMA user_version", [], |row| row.get(0))?;
213:            conn.query_row("PRAGMA user_version", [], |row| row.get(0))?;
221:            .and_then(|()| conn.pragma_update(None, "user_version", target));
1149:            .query_row("PRAGMA user_version", [], |row| row.get(0))
1330:            .query_row("PRAGMA user_version", []
[truncated]
- [evidence-3e08beed6ffbbee266849209] cargo exit: 0
suites_ok=16
failscan_exit=0
--- migration discriminators ---
57:test store::tests::migrations_are_idempotent_and_vec_loads ... ok
60:test store::tests::concurrent_migration_of_a_shared_fresh_database_succeeds ... ok
